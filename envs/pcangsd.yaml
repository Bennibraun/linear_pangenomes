name: linear-pangenomes-pcangsd
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11
  - pcangsd >=1.10
  - plink >=1.90b6
  - bcftools >=1.15  # for clean VCF sample names in samples.txt
