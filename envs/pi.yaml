name: linear-pangenomes-pi
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - vcftools
  - bcftools >=1.15   # sample list per population for --keep
  - pandas            # concatenate per-population vcftools outputs
